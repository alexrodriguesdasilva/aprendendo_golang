/*
- Use var para declarar três variáveis. Elas devem ter package-level scope. Não atribua valores a estas variáveis. Utilize os seguintes identificadores e tipos para estas variáveis:
    1. Identificador "x" deverá ter tipo int
    2. Identificador "y" deverá ter tipo string
    3. Identificador "z" deverá ter tipo bool
- Na função main:
    1. Demonstre os valores de cada identificador
    2. O compilador atribuiu valores para essas variáveis. Como esses valores se chamam?
*/

package main

import (
	"fmt"
)

//Declarando três variáveis em package-level scope utilizando var
var x int
var y string
var z bool

func main() {

	//Demostrando valores de cada identificador
	fmt.Printf("O valor da variável X é: %v\n", x)
	fmt.Printf("O valor da variável Y é: %v\n", y)
	fmt.Printf("O valor da variável Z é: %v\n", z)

	fmt.Printf("\n Os valores atribuidos as variáveis foram os valores zeros, isso acontece pois não atruimos valores a nenhuma variável \n")
}